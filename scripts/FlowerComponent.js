// Convert a raw data flower to an HTML representation of that flower
const SingleFlowerComponent = (flower) => {
    return `
        <section class="flower">${flower}</section>
    `
}

export default SingleFlowerComponent
